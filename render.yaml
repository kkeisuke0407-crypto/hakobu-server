services:
  - type: web
    name: hakobu-server
    env: node
    branch: main
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
